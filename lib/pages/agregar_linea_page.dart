// AgregarLineaPage removed: this project primarily serves static web pages.
// Kept as an inert stub to avoid Dart analyzer errors if Flutter tooling runs.
// If you want this page restored as a Flutter UI, re-add the implementation.

// No runtime code required for web-only operation.
