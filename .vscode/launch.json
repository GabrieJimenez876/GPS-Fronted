{
    // Usa IntelliSense para conocer los atributos disponibles.
    // Coloca el cursor sobre un atributo para ver su descripción.
    // Para más información: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Abrir index.html (workspace)",
            // Uses workspace relative path so it works on any machine
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}